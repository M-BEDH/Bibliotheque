{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/navbar.css') }}">
{% endblock %}

<nav class="navbar navbar-expand-lg bg-body-tertiary">
	<div class="container">

		<!-- Bouton burger -->
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarScroll"
			aria-controls="navbarScroll" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="navbarScroll">
			<ul class="navbar-nav me-auto mb-2 mb-lg-0 navbar-nav-scroll" style="--bs-scroll-height: 100px;">

				<!-- Accueil -->
				<li class="nav-item">
					<a class="nav-link active" href="{{ path('app_home') }}">Accueil</a>
				</li>

				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="{{ path('app_categories_index') }}" role="button" data-bs-toggle="dropdown">Catégories</a>
					<ul class="dropdown-menu">
					    <li><a class="dropdown-item" href="{{ path('app_categories_index') }}">Liste des catégories</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_categories_new') }}">Ajouter une catégorie</a></li>
					</ul>
				</li>

                <li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="{{ path('app_auteur_index') }}" role="button" data-bs-toggle="dropdown">Auteurs</a>
					<ul class="dropdown-menu">
					    <li><a class="dropdown-item" href="{{ path('app_auteur_index') }}">Liste des auteurs</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_auteur_new') }}">Ajouter un auteur</a></li>
					</ul>
				</li>

                <li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="{{ path('app_books_index') }}" role="button" data-bs-toggle="dropdown">Livres</a>
					<ul class="dropdown-menu">
					    <li><a class="dropdown-item" href="{{ path('app_books_index') }}">Liste des livres</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_books_new') }}">Ajouter un livre</a></li>
					</ul>
				</li>
      
            </ul>
        </div>
    </div>
</nav>
         
